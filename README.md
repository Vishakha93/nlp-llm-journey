# nlp-llm-journey
Learning log and code playground for my journey from core NLP concepts (tokenization, BPE, language modeling) to transformer internals and large language model systems (GPU memory, model parallelism, LoRA, deployment).
